task settings {
    project.ext.devWorkspace = true
}

for (subproject in subprojects) {
    evaluationDependsOn(subproject.path)
    if(subproject.name.equals("zworkspace")) {
//        subproject.updateAT()
    }
}